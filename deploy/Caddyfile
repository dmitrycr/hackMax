{$GATEWAY_ADDRESS: :80} {
    encode gzip
    @api path /api/* /healthz /readyz
    handle @api {
        reverse_proxy api:8080
    }
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}

