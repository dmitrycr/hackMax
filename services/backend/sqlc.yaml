version: '2'
sql:
  - engine: postgresql
    schema: db/migrations
    queries: db/queries
    gen:
      go:
        package: dbgen
        out: internal/platform/postgres/dbgen
        sql_package: pgx/v5

